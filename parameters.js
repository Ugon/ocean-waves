var WIDTH = 		512;
var HEIGHT = 		512;

var A_POSITION_INDEX = 0;

var UNIT_TEXTURE_RANDOM_NORMAL_PAIR = 0;

var randomNormal = function() {
    return (Math.random() + Math.random() + Math.random() + Math.random() + Math.random() + Math.random() - 3) / 3;
}